chore: report v8.0, correct the TCB table, and drop every em dash

Version. src/main.py had "7.0.0" inlined at four sites on a v8-hardened tree,
so /health answered 7.0.0. There is now a single PAAC_VERSION constant, used by
the OpenAPI metadata and by /health and nowhere else. README title and the
Docker tag follow; the tag still said v6.1.

TCB table. Replaced "~2,400 lines across six core modules" with the measured
four-module, 2,101-line list that protect_tcb() actually enforces, and stated
that sil_compiler.py sits outside it while performing the recursion and
loop-bound checks the safety argument rests on.

Four README claims the artifact does not support are corrected in place rather
than quietly dropped: the self-verification stubs are tautologies whose
assertion is implied by their own guard; the bounded-loop Z3 step is a
per-loop certificate, not a proof, since it asserts that a literal already
capped by the parser exceeds the cap; /attest/verify checks a record against
the key embedded in that same record, so it proves consistency and not
provenance; and PCM certificates are symmetric HMAC with a public default key.
Known Limitations is rewritten and ordered by severity, composition first.

Em dashes. 447 removed across 62 files, plus .env.example, .gitignore and
examples/backdoor.sil, which an extension filter had missed. PLAN_V8 section 3
forbids them repo-wide and AUDIT_FINDINGS recorded that pre-existing prose
still carried them. Spaced dashes became commas, headings became colons.

Verified: 398 passed, 2 xfailed, exit 0, and compileall clean on src and tests.